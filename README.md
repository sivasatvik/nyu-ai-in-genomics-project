# Environmental Adaptation Modeling

Use the notebook below for a **single-run workflow** (download data → build dataset → train → evaluate):

- `notebooks/end_to_end_alien_genomics_pipeline.ipynb`

This notebook contains all sections in one place so you can run it top-to-bottom on your GPU machine.
